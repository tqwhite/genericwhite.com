{{#if bookmarkGrid.isResolved}}
	{{setDefaultGrid}}
		<div class='gridContainer'>
	{{#each bookmarkGrid.value}}
	
	<div class='gridTitle'>
	{{#each grids}}
			<span class='gridButton {{#is refId visibleGridRefId}}highlight{{/is}}' ($click)='chooseGrid(refId)'>{{name}}</span>
	
	{{/each}}
	</div>
			
	{{#each grids}}
		{{#is refId visibleGridRefId}}
			{{showGrid(.)}}
			{{/is}}
	{{/each}}
	{{/each}}
		</div>
{{/if}}